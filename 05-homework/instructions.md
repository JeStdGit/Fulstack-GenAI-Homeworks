# תרגול פקודות Git בסיסיות

**מטרה:** תרגול פקודות Git בסיסיות לשמירת עבודה מקומית, תיקון טעויות וחיבור ל-GitHub (ללא התעסקות עם ענפים או התנגשויות).

## משימה 1: מכונת הזמן המקומית ותיקון טעויות
**תרחיש:** אתם מתחילים פרויקט חדש, אבל עושים כמה טעויות נפוצות בדרך. בואו נשתמש ב-Git כדי לתקן אותן!

* **התחלת מעקב:** צרו תיקייה חדשה במחשב, פתחו אותה ב-VS Code, פתחו את הטרמינל, ואתחלו את Git (`git init`).
* **שמירה ראשונה:** צרו קובץ בשם `index.html`. הוסיפו אותו לאזור ההכנה (staging area) באמצעות `git add index.html` (או `git add .`), ולאחר מכן שמרו אותו באופן קבוע באמצעות:
  ```bash
  git commit -m "Create index.html"
  ```
* **רגע ה-"אופס" (ביטול שינויים):**
  פתחו את `index.html` והקלידו ג'יבריש אקראי ששובר את הקוד. שמרו את הקובץ. במקום לתקן את זה ידנית, תגידו ל-Git לשחזר באורח פלא את הקובץ לאיך שהוא נראה ב-commit האחרון שלכם באמצעות:
  ```bash
  git restore index.html
  ```
* **הסוד האקראי:**
  צרו קובץ בשם `passwords.txt`. בטעות הוספתם הכל לאזור ההכנה על ידי הפעלת `git add .`.
  עדיין לא עשיתם commit! הסירו את `passwords.txt` מאזור ההכנה מבלי למחוק אותו מהמחשב שלכם באמצעות:
  ```bash
  git rm --cached passwords.txt
  ```

## משימה 2: שליחת קוד לענן
**תרחיש:** הקוד המקומי שלכם בטוח, אבל עכשיו אתם רוצים לגבות אותו ב-GitHub.

* **יצירת תיקיית הענן:** היכנסו ל-GitHub.com וצרו מאגר (repository) חדש וריק (אל תסמנו "Add a README file").
* **קישור ביניהם:** בטרמינל של ה-VS Code שלכם, חברו את התיקייה המקומית למאגר ה-GitHub החדש שלכם באמצעות הפקודה ש-GitHub מספק:
  ```bash
  git remote add origin <YOUR_GITHUB_REPO_URL>
  ```
  *(הערה: ודאו שהענף הראשי שלכם נקרא 'main' על ידי הפעלת `git branch -M main`)*
* **העלאת העבודה שלכם:** שלחו את ה-commits המקומיים שלכם לשרת של GitHub בפעם הראשונה על ידי הפעלת:
  ```bash
  git push -u origin main
  ```

## משימה 3: עבודת צוות ומחשבים חדשים
**תרחיש:** קניתם מחשב נייד חדש (או שאתם עובדים עם חבר לצוות). אתם צריכים להוריד את הקוד ולקבל את העדכונים האחרונים.

* **הורדת הפרויקט:** צרו תיקייה חדשה לגמרי וריקה במחשב שלכם (זה מייצג את "המחשב הנייד החדש" שלכם). פתחו טרמינל בתיקייה החדשה הזו והורידו את כל הפרויקט שלכם מ-GitHub באמצעות:
  ```bash
  git clone <YOUR_GITHUB_REPO_URL>
  ```
* **דימוי עבודה של חבר צוות:**
  היכנסו למאגר שלכם ב-GitHub.com בדפדפן.
  לחצו על `index.html`, לחצו על סמל העיפרון ✏️ כדי לערוך אותו ישירות באתר, הקלידו "Hello from the cloud!", ולחצו על "Commit changes".
* **הורדת העדכונים:**
  חזרו לטרמינל ה-VS Code שלכם (בתוך התיקייה ששכפלתם). המחשב המקומי שלכם עדיין לא יודע על השינויים שנעשו ב-GitHub. משכו את העדכונים האחרונים למחשב שלכם על ידי הפעלת:
  ```bash
  git pull
  ```

---
**כל הכבוד!** ניהלתם בהצלחה את הקוד שלכם באופן מקומי, גיביתם אותו לענן וסנכרנתם את העבודה שלכם.