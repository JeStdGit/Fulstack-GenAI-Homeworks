export const getRandomInteger = (min = 0, top = 1000) => Math.floor(Math.random() * (top - min + 1)) + min
export const getRandomNumber = (min = 0, top = 1000) => Math.random() * (top - min + 1) + min